# OCaml / Dune
_build/
*.install
*.merlin
*.cmt
*.cmti
*.cmx
*.cmxa
*.o
*.a
*.so
.opam-switch/

# Node / Playwright / assets
node_modules/
playwright-report/
test-results/
coverage/
dist/

# Python helper/cache spillover
__pycache__/
*.py[cod]
.pytest_cache/

# Local env / secrets
.env
.env.*
!.env.example
*.local

# Data and generated artifacts
data/*.duckdb
data/*.duckdb.wal
artifacts/
tmp/
logs/
*.log

# Docker / OS
.docker/
.DS_Store
Thumbs.db

# AI Workflow System (proprietary — do not publish)
# ⚠️ TRACKED DURING DEV — run /prepare-public before making repo public
# .agent/workflows/
# .agent/guides/
# .agent/agents/
.agent/.last-sync

# Internal project planning
# ⚠️ TRACKED DURING DEV — run /prepare-public before making repo public
# docs/progress.md
# docs/yolo-inbox.md
# docs/build-journal.md
# docs/build-journal/
docs/architect_journal.md
# docs/*PRD*.md
# docs/*prd*.md

# Klevar local operational tooling — managed by /refresh and never app source
tools/klevar-yolo-runtime/
tools/klevar-pi-package/
.yolo/refresh/
.yolo/runtime/
.yolo/incidents/
.yolo/runtime-lock.json
.yolo/runtime-pid.json
.yolo/command-cache.json

# YOLO runtime generated artifacts (always ignored)
.yolo/worktrees/
.yolo/pi-sessions/
.yolo/subagent-prompts/
.yolo/subagent-runs/
.yolo/events/
.yolo/runtime-state.json

# Pi local browser/mesh artifacts
.pi/browser/
.pi/agents/

# YOLO Mode workspace — audit trail
# ⚠️ TRACKED DURING DEV — run /prepare-public before making repo public
# ⛔ NEVER write production API keys, tokens, passwords, or any real
#    credential to ANY file under .yolo/ — these files commit to git
#    history on every YOLO push. Use os.environ[...] / process.env[...]
#    / ${VAR} instead. The pre-commit secret scanner (scripts/scan-secrets.ps1)
#    will block commits containing real-shape secrets, but the only safe
#    pattern is to never write them in the first place.
# .yolo/

# Multi-agent platform context
# CLAUDE.md / AGENTS.md / .cursorrules are thin pointer files regenerated by /sync-context — leave ignored
# Rules and knowledge are NOT mirrored into .claude/ — pointer files reference .agent/rules/_index.md directly
CLAUDE.md
AGENTS.md
.cursorrules
# ⚠️ TRACKED DURING DEV — run /prepare-public before making repo public
# .claude/
.claude/settings.local.json
.claude/projects/
.claude/ide/
.claude/shell-snapshots/
.claude/todos/
.claude/statsig/
